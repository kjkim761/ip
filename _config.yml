plugins:
  - jekyll-remote-theme
remote_theme: cotes2020/jekyll-theme-chirpy
exclude:
  - "exclude.md"
toc:
    enabled: true
    h_min: 1
    h_max: 99
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "page"
    
